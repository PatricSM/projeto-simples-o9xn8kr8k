/**
 * Wrapper trivial para a página de Auth (tab "login" pré-selecionada).
 * A lógica de autenticação vive em src/pages/Auth.tsx.
 */
import Auth from './Auth'

export default function Login() {
  return <Auth />
}
