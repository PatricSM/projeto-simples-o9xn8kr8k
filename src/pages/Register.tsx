/**
 * Wrapper trivial para a página de Auth (tab "signup" pré-selecionada).
 * A lógica de cadastro vive em src/pages/Auth.tsx.
 */
import Auth from './Auth'

export default function Register() {
  return <Auth />
}
